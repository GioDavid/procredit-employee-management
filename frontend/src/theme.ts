import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    primary: { main: '#003d78' },
    secondary: { main: '#e30613' },
    background: { default: '#f4f6f8' },
  },
  shape: { borderRadius: 8 },
})
